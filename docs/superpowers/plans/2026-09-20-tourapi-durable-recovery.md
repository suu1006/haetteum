# TourAPI durable response capture and bounded recovery

Spec: User approved TourAPI receive -> durable staging -> validate/deduplicate -> update existing/insert new -> replay processing failures from staged JSON; API retries only for unsuccessful requests. docs/tourapi-calls.md remains binding. Daily requests 1000, tourism700/festival300; rows are not requests. Previous reliability investigation is background, not an instruction to wait for another deployment before implementing recovery.

Global constraints: Only TourApiClient performs HTTP inside TourApiPolicy batch. Preserve existing place IDs/relations, good snapshots and list checkpoints. Never log service keys/full URLs/raw response contents. Additive migration; no live API during tests. Worktree only. TDD with focused tests and actual PostgreSQL integration. Do not relax provider schemas without actual evidence.

## Task 1: Durable capture and local replay through both sync services

Implement response staging and persistent item recovery end to end, in tourism API code, Prisma additive migration and tests. Use existing services/ports where possible.

- Persist bounded raw response text and safe request identity before JSON/schema parsing, with job, operation, safe params, source version/item identity or list-run identity, HTTP status, captured timestamp and lifecycle state. No URL/credentials/headers; redact echoed configured secret. Staging DB failure must stop collection, never be mislabeled/retried as network failure. Store provider error evidence but never reuse it as successful response. Retention cleanup must be bounded and documented; unresolved records must not disappear silently.
- Use version-scoped item context and per-operation captures. Same item/version processing failure reuses captured successful envelopes, reparses with current schemas; missing operation alone may call API. Validation failures retain raw evidence for parser fixes; provider transient failures must permit fresh retry. No stale-version replay. List requests capture before transformation/upsert, reuse only pending failed processing for correct run/date/params; successful old daily lists cannot suppress fresh collection. If list replay can no longer safely advance checkpoint, rerun fresh list without checkpoint mutation; document distinction.
- EnsureCapacity must count only missing required operations and allow entirely local replay with exhausted HTTP budget. Missing/nonvalid responses still require budget. Service/source-version race must not mark changed data complete. Stage complete only after destination transaction committed; crash between commit and completion must remain idempotent.
- Persist per job/contentId/sourceVersion failures with stage, safe classified code, attemptCount,nextAttemptAt,state. Failed item waits1day then3days then quarantined after third failed execution; deferred budgets do not increment failure. New versions eligible. Fresh/unattempted items run before due failed items to avoid starvation; failures retain existing good data. DB connectivity/system errors stop batch instead of per-item HTTP waste.
- Add bounded CLI local replay of failed IDs (default no HTTP for local replay; explicit fetch-missing mode inside policy); IDs/limit/maxrequests validated; quarantine can be explicitly requeued. Output safe summary, not raw bodies. Keep existing CLI modes compatible.
- Tests must prove both tourism and festival DB/mapping failure then retry with zero new HTTP and same destination ID; partial capture only fetches missing operations; new version cannot reuse old; raw invalid schema persists before validation; list DB failure replay preserves checkpoint and successful previous-day list not reused; staging DB unavailable stops; failures do not starve unseen; budget-deferred no failure count. Create meaningful PostgreSQL tests for persistence after new repository instance and unique/idempotent state.
- Register all providers in TourismModule. Update docs/tourapi-calls.md with exact tables and replay commands actually implemented. Do not implement timeout/retry policy changes owned by Task2 yet, but expose recovery context isRetry for it. No subagents. Commit implementation and tests, write report with RED/GREEN commands/results and interfaces for Task2.

## Task 2: Bounded network retries and durable recovery quota

Build on Task1. TourApiClient max3attempts total, delay2s then10s plus0..500msjitter; honor Retry-After (seconds/date) without earlier retry. Retry network/TIMEOUT/5xx and body01/05/23; never auth20/30/31/401/403 or quota22. Response timeout20s includes body and bounds promises even if body hangs. API retries reserve actual requests exactly once.

- Durable retry_calls column in job daily ledger; tourism70/festival30 within existing700/300; previous failed item's HTTP and extra attempts count once as retry. Local replay costs0. Retry exhaustion defers that recovery while fresh work remains possible. Preserve advisory lock and completion spacing.
- Persist provider cooldown for repeated429/23 (15minutes minimum or longer Retry-After), quota22 until next KST day, auth until bounded next run/manual correction; no retries per item after fatal stop. Long Retry-After >60s defers without sleeping earlier. Batch40minute deadline, preflight/request honor it; no unbounded lock/body waiting introduced.
- Tests use fake clocks/sleep and Postgres for restart/concurrent quota enforcement; cover retrybudget independent of mainbudget, no counter on denial, source-version context and failed prior item accounting. Ensure policy errors don't get sanitized into NETWORK_ERROR.
- Commit and report; no subagents.

## Task 3: Reviewable reporting and final verification

Report actual list/detail states, successful/failed/pending/quarantined/local-replayed counts and safe stop reason through CLI/logs and Notion supported schema. Distinguish partial detail failure in message without requiring an unapproved external Notion schema edit; existing FAILED remains when failedCount>0, DEFERRED only pure postponement. Error summaries safe bounded. Document operations: inspect failed IDs, local replay, fetch missing within limits, retention, change-version behavior. Add scheduler/recorder regressions. Run full API units, API lint/build, isolated DB suite, applicable workspace checks. Final independent review. Create PR and use existing deployment workflow with compatibility guard; no live full batch merely for testing. State historical103 cause remains unproven until new captured evidence.
